(()=>{"use strict";!function(A){let e=new Image;e.onload=e.onerror=function(){!function(A){let e=!0===A?"webp":"no-webp";document.documentElement.classList.add(e)}(2==e.height)},e.src="data:image/webp;base64,UklGRjoAAABXRUJQVlA4IC4AAACyAgCdASoCAAIALmk0mk0iIiIiIgBoSygABc6WWgAA/veff/0PP8bA//LwYAAA"}()})();




/*mask-phone==============================================================================================================*/
//https://imask.js.org/guide.html
let element = document.getElementById('phone');
let maskOptions = {
   mask: '+38(000)000-00-00',
   lazy: false
};
let mask = new IMask(element, maskOptions);


/*countdown==========================================================================================================*/
let allTime = 2;
let time = 3600 * allTime;

const countdownEl = document.getElementById('countdown');

setInterval(updateCountdown, 1000);

function updateCountdown() {
   let hours = Math.floor(time / 60 /60)
   let minutes = Math.floor(time / 60 / allTime);
   let seconds = time % 60;
   minutes = minutes > 60 ? minutes: minutes;
   seconds = seconds < 10 ? '0' + seconds: seconds;
   countdownEl.innerHTML = `${hours}:${minutes}:${seconds}`;
   time--;
}


/*date==================================================================================*/
let date = document.querySelector('.form__date');
let today = new Date();
let dd = String(today.getDate()).padStart(2, '0');
let mm = String(today.getMonth() + 1).padStart(2, '0');
let yyyy = today.getFullYear();

today = `Order date: ${dd}.${mm}.${yyyy}`;
date.innerHTML = today;